#include <stdio.h> //add subtract and mul

int main()
{

    int n1, n2, add, sub, mul;
    n1 = 5;
    n2 = 1;
    add = n1 + n2;
    sub = n1 - n2;
    mul = n1 * n2;
    div = n1 / n2

                   printf("addition: %d\n", add);
    printf("subtraction: %d\n", sub);
    printf("multiplication: %d", mul);
    printf("multiplication: %d");

    return 0;
}